{
  ,
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" }
  ]
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "docs" }
    }
    ,
    {
      "src": "api/**/*.ts",
      "use": "@vercel/node"
    }
  ]
    "routes": [
      { "src": "/api/(.*)", "dest": "/api/$1" }
    ]
